// Retired view — the /ai-consulting and /services/ai-automation routes now
// permanently redirect (see vercel.json and the app router pages).
// Stub kept so any stray imports still compile.
const AIConsulting = () => null;

export default AIConsulting;
