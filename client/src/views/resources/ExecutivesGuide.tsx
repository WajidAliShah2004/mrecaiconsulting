// Retired view — /resources/executives-guide-to-ai now permanently redirects
// to /resources (see vercel.json and the app router page).
// Stub kept so any stray imports still compile.
const ExecutivesGuide = () => null;

export default ExecutivesGuide;
