// Retired view — /tools/roi-calculator now permanently redirects to
// /resources (see vercel.json and the app router page).
// Stub kept so any stray imports still compile.
const ROICalculator = () => null;

export default ROICalculator;
