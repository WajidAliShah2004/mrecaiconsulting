// Retired view — /tools/readiness-assessment now permanently redirects to
// /book-now (see vercel.json and the app router page).
// Stub kept so any stray imports still compile.
const ReadinessAssessment = () => null;

export default ReadinessAssessment;
